import 'package:flutter/widgets.dart';

class RaitingScreen extends StatelessWidget {
  const RaitingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Center(
        child: Text('Raiting Screen'),
      ),
    );
  }
}
